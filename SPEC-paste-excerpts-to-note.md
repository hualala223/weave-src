# Spec: 粘贴所选摘录到笔记——单选/多选摘录按最早在前写入笔记文档末尾 (Paste Excerpts to Note)

Status: Finalized locally — 设计经两轮 grilling 定稿、用户已确认测试接缝（单一纯函数服务缝：块构建收拢进服务，宿主仅留装饰与插入胶水）。**待发布 issue tracker**（本机无 gh CLI / token / tracker 配置；配置好后按 `ready-for-agent` 标签发布）。实现等用户明确开工指令。

## Problem Statement

阅读器的侧栏「摘录」面板是回看所有划线的窗口，但目前对面板内的摘录只提供「删除」这一种批量操作（单选或多选后删除）。读者读到一段想收进读书笔记的内容时，只能回到书里重新划线触发「自动插入」，或手动复制面板卡片文本——后者丢掉引用块格式与深链。面板本身完全无法把已有摘录搬进笔记。

另一个顺序陷阱：摘录面板按 **最新摘录在上** 显示（createdTime 降序），而把多条摘录粘贴进笔记时，读者期望 **最早摘录在最上**（与阅读先后一致），可直接照面板顺序复制则会得到倒序。

用户诉求：在摘录面板里单选或多选摘录，点一个新按钮，即以与阅读界面划线自动粘贴**完全相同的格式**把所选摘录写入打开的笔记文档；多条时最早摘录在最上。

## Solution

摘录面板新增「粘贴所选摘录到笔记」动作，两个入口：

- **多选**：批量选择浮条新增粘贴按钮 + 批量右键菜单项「粘贴所选摘录到笔记」；
- **单选**：摘录卡片右键菜单新增「粘贴到笔记」（单条直粘，不进批量模式）。

行为与「自动插入」同款：逐条按同一引用块格式构建（深链、章节标签、样式位、来源笔记路径、字色装饰），**追加到最近激活的 Markdown 笔记文档末尾**（末行非空先补换行）；多条一次写入（单一撤销点）。

关键语义：

- **顺序**：按摘录 createdTime **升序**（最早摘录在最上），与面板最新在上的显示顺序相反；同一时间戳保持面板相对顺序（稳定）；缺失 createdTime 的视为最早。
- **时间戳**：块后缀时间戳（开启「添加时间」时）取**摘录原始创建时间**，而非粘贴时刻；想法条目时间戳同样取摘录创建时间（面板不单独记录想法写入时刻）。
- **想法**：带想法的摘录，其 想法条目 直接渲染进块（所见即所得），不依赖事后想法入笔记合并。
- **格式细节**：块头深链携带摘录真实 eid；章节标签遵循「章节标签格式」设置（root/leaf/full），摘录自带章节标题兜底；下划线/删除线/波浪线条目保留样式 token，普通高亮不输出；摘录内 字色标记 保持彩色装饰（与自动插入一致）。
- **无打开的 Markdown 编辑器**：仅提示「未找到活动的 Markdown 编辑器」，不隐式复制。
- **粘贴后**：批量路径成功后**自动取消实际写入成功条目的勾选**（按回传 keys 剔除；构建失败的条目保持勾选以便立即处理；整批未写入——如无编辑器——不动任何勾选），面板**保持批量选择模式**（可继续勾选再粘），并提示「已粘贴 N 条摘录到笔记末尾」；单条路径直接完成。
- **不去重**：照贴，与自动插入一致。

目标块形态（多条各为一块，块间空行分隔，最早在前）：

```
> [!EPUB|underline] [[书#…weave-loc…&eid=A|书]] [第一章] 2025-01-01 10:20
> 最早划的原文
>
> 💡 01-01 10:20
> 这条摘录的想法

> [!EPUB] [[书#…weave-loc…&eid=B|书]] [第三章] 2025-01-03 21:05
> 后来划的原文
```

## User Stories

1. 作为读者，我想要在卡片右键菜单直接「粘贴到笔记」单条摘录，以便不进入批量模式也能最快地把一条摘录搬进笔记。
2. 作为读者，我想要在批量选择模式里点浮条粘贴按钮一次写入多条摘录，以便批量收拢摘录到笔记。
3. 作为读者，我想要多条摘录粘贴时**最早摘录在最上**（createdTime 升序，与面板最新在上的显示相反），以便笔记里的顺序与我的阅读先后一致。
4. 作为读者，我想要粘贴出的块与划线自动粘贴**完全同款格式**（引用块、深链、章节标签、样式位、来源笔记路径），以便两类来源的块外观统一、点击都能跳回书中。
5. 作为读者，我想要带想法的摘录在粘贴时把 想法条目 一并写进块里，以便搬进笔记的摘录不丢批注。
6. 作为读者，我想要块后缀时间戳显示摘录的原始创建时间（受「添加时间」开关约束），以便笔记反映「何时划的」而非「何时粘的」。
7. 作为读者，当没有打开任何 Markdown 编辑器时，我想要只收到提示而不写入也不静默复制，以便行为可预期、不产生隐式副作用。
8. 作为读者，我想要批量粘贴成功后**自动取消已写入条目的勾选**并保持选择模式，以便不必手动逐一取消勾选，也能继续勾选其他摘录再粘、不必反复退出重进。
9. 作为读者，我想要未选中任何摘录或粘贴进行中时按钮禁用，以便不会误触或重复提交。
10. 作为读者，我想要粘贴**不自动去重**（与自动插入一致），以便行为简单一致，误粘可用 Obsidian 撤销。
11. 作为读者，我想要该按钮固定执行「插入到笔记末尾」，不跟随「自动插入」(zap) 开关切换插入/复制，以便语义单一明确。
12. 作为读者，我想要下划线/删除线/波浪线条目的块头保留对应样式 token、普通高亮不带样式 token，以便块外观与划线类型一致。
13. 作为读者，我想要摘录内 字色标记 在粘贴的块里保持彩色装饰，以便笔记中的原文与书内所见一致。
14. 作为读者，我想要块头章节标签遵循「章节标签格式」设置（root/leaf/full），摘录自带章节标题兜底，以便与既有摘录输出一致。
15. 作为读者，我想要在搜索/筛选后的面板里只粘贴「当前结果中已勾选」的摘录，以便与删除所选的口径一致。
16. 作为读者，当某条摘录缺失创建时间时，我想要它排在最早位置；开启「添加时间」时该条不带时间戳后缀，以便不显示错误时间。
17. 作为读者，我想要同一时间戳的多条摘录保持面板相对顺序，以便不出现无意义的乱序。
18. 作为读者，我想要多行摘录逐行转为引用行，以便长摘录的换行结构不丢。
19. 作为读者，我想要多条一次写入（单一撤销点），以便误操作可以整体撤销。
20. 作为读者，我想要粘贴成功后收到「已粘贴 N 条摘录到笔记末尾」提示，以便确认动作完成。
21. 作为读者，即使某条摘录构建异常（如文本缺失），我想要其余摘录照常粘贴、失败条目不阻塞整体，以便局部问题不拖垮整批。
22. 作为读者，我想要单条粘贴完成后卡片菜单正常关闭、无残留状态，以便界面干净。
23. 作为读者，当批量粘贴整批未写入（如没有打开任何 Markdown 编辑器）时，我想要的勾选集合原样保留，以便明确知道这一批没有进笔记。

## Implementation Decisions

- **单一新模块（唯一新测试缝）：「批量粘贴块构建」纯函数服务**。输入为摘录列表（面板快照形状：cfiRange / text / color / chapterIndex / chapterTitle / excerptId / createdTime / noteTypeKey / commentText / hasCommentDivider）与构建上下文（书文件路径、书 sourceId、章节标签格式与截断长度、是否加时间戳、笔记文档 sourcePath、逐条已装饰文本），输出为可直接插入笔记末尾的完整块文本（多块以空行分隔）。服务内部承担全部可测行为：
  - createdTime 升序（缺失视为最早、并列稳定）；
  - 逐条构建引用块（深链与 eid、样式位 token、块后缀时间戳口径＝摘录原始创建时间）；
  - 带想法摘录内联渲染 想法条目（条目时间戳＝摘录创建时间）；
  - 空块丢弃、去尾换行、块间空行分隔的拼接。
  不含 Obsidian 依赖；链接构建器等以参数注入。
- **宿主只留薄胶水（不构成新测试缝）**：从面板快照取摘录，逐条做字色装饰（既有字体标记链路）与章节标签解析（按「章节标签格式」设置经引擎解析，失败兜底摘录自带标题），把结果交给块构建服务，再经既有「追加到文档末尾」插入工具写入最近激活的 Markdown 笔记文档；无编辑器仅提示（复用既有文案），不复制。成功提示由宿主弹出。
- **粘贴后自动取消勾选（2026-09-22 修订）**：批量路径写入成功后，宿主按块构建服务回传的 `keys`（= cfiRange 口径）从面板 `selectedKeys` 剔除已写入条目——与「已粘贴」标记回写共用同一成功口径；构建失败的条目保持勾选；整批未写入时不动勾选。面板保持在批量选择模式。
- **面板接线**：批量选择浮条新增粘贴按钮（图标、禁用条件：未选中 / 宿主回调缺失 / 粘贴进行中）、批量右键菜单项「粘贴所选摘录到笔记」、卡片右键菜单项「粘贴到笔记」（单条，不进批量模式）。批量路径粘贴成功后保持选择模式，并按上方「粘贴后自动取消勾选」口径剔除已写入条目。
- **共享状态**：阅读器宿主向侧栏面板新增暴露粘贴回调（携带摘录列表，返回是否成功），与既有删除回调同级，仅在摘录能力可用时提供。
- **顺序/时间戳口径（均已确认）**：升序按 createdTime；粘贴固定为插入，不跟随「自动插入」开关；不自动去重。
- **术语已固化**：「粘贴所选摘录」已录入 CONTEXT.md，并明确了两个入口、顺序反转、不去重、无编辑器仅提示等语义，与「自动插入」「想法入笔记」区分。

## Testing Decisions

- **好测试的标准**：只测外部行为——给定摘录列表与构建上下文，断言输出的完整块文本（顺序、想法条目、时间戳口径、样式位、空块丢弃、拼接分隔）；不给内部实现留断言，不测宿主的装饰/插入胶水。
- **测试模块**：批量粘贴块构建服务（唯一新缝）。
- **先例**：
  - `idea-note-doc.test.ts`——纯函数文档变换的断言风格（给定输入→断言输出文本/枚举）；
  - `note-editor-insert.test.ts`——依赖注入 + 伪编辑器的风格；
  - 工作区已有的 `excerpt-batch-paste.test.ts`（排序/拼接先行用例）在实现阶段收拢并扩展为该缝的测试。
- **不单测**：svelte 组件胶水（面板按钮/菜单）与宿主胶水，沿仓库惯例（UI 胶水不单测）；字色装饰、插入工具、深链构建均有既有测试覆盖，不在本缝重复。

## Out of Scope

- 「复制所选摘录到剪贴板」的并行入口（当前需求仅为粘贴到笔记）。
- 跟随「自动插入」开关在插入/复制间切换。
- 粘贴去重、增量同步、重复检测。
- 单独记录并展示「想法写入时刻」（面板数据模型无此字段，取摘录创建时间）。
- 图片记录、书签备注等非划线条目的批量粘贴。
- 搜索结果 / 书签面板的批量粘贴。
- 面板内撤销历史管理（依赖 Obsidian 原生撤销）。
- 国际化/文案配置化。

## Further Notes

- 与既有输出链路的区分：**自动插入** 发生于划线创建时；**想法入笔记** 发生于想法保存时；**粘贴所选摘录** 发生于用户从面板主动挑选时。三者输出均为摘录块、格式一致，可安全共存。
- 依赖均为既有能力：深链构建与 eid 序列化、字色装饰、章节标签格式解析、末尾追加插入，均有测试覆盖。
- 工作区存在一份对齐草稿实现（未提交 git）：含「排序/拼接」纯助手与宿主内的块构建胶水；按本规格收拢为单一服务缝后即与规格一致。
- 发布说明：本机无 gh CLI / tracker 配置，按仓库先例（SPEC-bookmark-notes / SPEC-idea-to-note）在 Status 标注「待发布 issue tracker」，配置好后按 `ready-for-agent` 标签发布。