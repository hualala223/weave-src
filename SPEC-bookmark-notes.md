# Spec: 书签备注——在书签上写下可编辑的备注感想 (Bookmark Notes)

Status: Finalized locally — 设计经多轮 grilling 定稿（Q1(a) 书签备注 / Q2(b) 多条备注串 / Q3(a) 仅存阅读器 / Q4(a) 行内展开编辑 / Q5(b) 全 CRUD / Q6(b) 最新在上 / Q7(a) 行点击跳转+常显铅笔 / Q8(c) 折叠行外观不变 / Q9(a) 仅侧栏 / Q10(a) 输入区顶部自动聚焦 / Q11(a) 就地替换 / Q12(b) 有备注删书签先确认），用户已确认单服务测试缝。**待发布 issue tracker**（本机无 gh CLI / token / tracker 配置；配置好后按 `ready-for-agent` 标签发布）。

## Problem Statement

侧栏书签列的每个书签只是「位置标记 + 章题 + 时间戳」的静态卡片，点下去只能跳回书中位置。读到值得标记的一页时，想在书签上顺手写下备注或感想却无处可写；书签记录（`EpubBookmarkRecord`）连一个可编辑文本字段都没有。

## Solution

每个书签可以挂一串**书签备注**（备注串）：在书签面板里点常显的铅笔按钮展开书签行，顶部自动聚焦的输入框写下多行备注保存后，备注插入到备注串最上方（最新在上），每条自带时间戳；已有备注可原地改写（时间戳与串内位置不变）或单条删除。备注只存于阅读器内（随书签一起持久化在每书聚合里），不进笔记文档、不进正文浮层。删除书签时备注一并删除；书签带有备注时删除需先确认，无备注照旧一击即删。整行点击跳转定位的行为保持不变。

## User Stories

1. 作为读者，我想要在书签行的铅笔按钮上点一下就能展开一个备注编辑区，以便在标记位置上写下备注或感想。
2. 作为读者，我想要展开后输入框自动聚焦、直接把光标停在输入区，以便「想写就写」、零额外点击。
3. 作为读者，我想要写出多行备注时换行结构原样保留，以便长感想不漏段落。
4. 作为读者，我想要保存备注后它出现在备注串的最上方（最新在上），以便最新想法一眼可见。
5. 作为读者，我想要每条备注自带 `MM-DD HH:mm` 时间戳，以便分辨不同时间写下的想法。
6. 作为读者，我想要保存空内容或纯空白内容的备注时什么都不发生，以便误触保存不会产生空壳备注。
7. 作为读者，我想要按 Esc 取消当前编辑、按 Ctrl/Cmd+Enter 保存，以便键盘不离手。
8. 作为读者，我想要再点一次铅笔（或收起操作）把展开区收回去，以便行恢复紧凑形态、不占面板空间。
9. 作为读者，我想要对已有备注点编辑后它在原位置变成文本编辑框，保存后恢复展示态，以便修改时不错乱上下文。
10. 作为读者，我想要编辑备注只改文本、不动它的原时间戳与串内位置，以便每页感想的「时间胶囊」锚点不被后来的修改污染。
11. 作为读者，我想要单条删除某条备注，以便清掉过时的感想而保留其余。
12. 作为读者，我想要书签折叠行保持现状外观（无备注徽标、无文本预览），以便本次改动不打扰既有视觉。
13. 作为读者，我想要整行点击仍是跳转到书签位置（脉冲闪烁 + 定位浮层），以便跳转肌肉记忆不受影响。
14. 作为读者，我想要书签和它的备注一起持久化，重新打开书后备注还在，以便感想不丢。
15. 作为读者，我想要对同一位置重复添加书签（upsert-by-CFI）时已有备注原样保留，以便合并书签不丢已写感想。
16. 作为读者，我想要删除无备注的书签照旧一击即删（无确认弹窗），以便轻量操作不被打断。
17. 作为读者，我想要删除带有备注的书签时先出现确认，以便误删不会连带清空一整串感想。
18. 作为读者，我想要备注只存在于阅读器侧栏书签面板中，既不会自动写进我的笔记文档，也不会改变正文里的划线/标注，以便书签备注是纯粹、私密的随手记。
19. 作为读者，我想要备注文本保持纯文本（不解析 Markdown、不改色），以便所见即所写、与划线的「想法」体验一致。
20. 作为读者，我想要备注数量没有写死上限，以便一个位置可以长期积累多轮感想。
21. 作为读者，我想要书签 tab 的搜索框行为不变（不搜索备注文本），以便清晰的 v1 边界。

## Implementation Decisions

- **模块改动**：书签服务（`EpubBookmarkService`）承担全部备注状态转移；侧栏书签面板组件承担展开/编辑交互；阅读器主组件经共享状态为面板提供备注操作回调并递增 bookmark 刷新版本（与现有书签删除路径同构）。
- **数据模型**：书签记录增加**可选**的备注数组字段，元素为「备注 id + 多行纯文本 + 创建时间戳」。字段可选、缺失按空串处理——旧 `weave-data.json` 直接兼容，**不做 schema 版本变更、不跑迁移**。
- **新增操作契约**（全部走既有每书串行化变更锁，落盘到同一 v2 每书聚合）：
  - 加备注：新备注插到备注串**首**（最新在上）；空/纯空白文本保存为 no-op（服务层与面板双重防护）；
  - 改备注：仅替换文本，**保留原 id、原时间戳、串内位置**；
  - 删备注：按备注 id 删除单条；
  - 删书签：整条书签记录（含备注串）一并删除。
- **upsert-by-CFI 契约强化**：`addBookmark` 重加同一位置时，除保留 `id`/`createdAt` 外，**必须显式保留已有备注串**——现实现会覆盖其余字段，这是实现此特性必须补上的坑。
- **面板交互**：折叠行外观与整行点击跳转不变；常显铅笔按钮展开行；展开区自上而下 = 自动聚焦的备注输入框（多行、Ctrl/Cmd+Enter 保存、Esc 取消）→ 备注串（新→旧，每条 `MM-DD HH:mm` + 文本 + 悬停出的编辑/删除动作）；编辑为就地替换（该条变 textarea + 保存/取消，保存后恢复展示态）；无备注时展开区显示空态提示；有备注的书签删除前先确认，无备注照旧直接删。
- **与「想法」的一致性**：快捷键（Esc 取消、Ctrl/Cmd+Enter 保存）与纯文本多行语义沿用划线的想法编辑；但编辑发生在面板行内而非正文浮层弹层。
- **测试缝（唯一）**：书签服务。所有备注状态转移（增/改/删、插顶排序、时间戳与位置不变、upsert 保留、空草稿 no-op、删书签连带删备注、旧数据无 notes 字段兼容）都在该缝断言。面板为薄 UI，沿用现有手动验证流程，不新增组件测试缝。

## Testing Decisions

- **好测试的标准**：只测外部可观察行为（持久化结果、串顺序、时间戳不变性、upsert 保留语义、空草稿 no-op），不测内部实现细节（如内部 id 生成算法、锁的实现）。
- **测试模块**：书签服务——唯一的逻辑缝。
- **先例**：现有 `EpubBookmarkService.test.ts`（vitest + obsidian mock + 内存 adapter + 预置 v2 聚合种子）——扩展同一文件、同一模式，覆盖备注的增改删与 upsert 保留回归。

## Out of Scope

- 正文浮层中的书签/备注标记（书签今天不渲染浮层，v2 候选）。
- 备注同步进笔记文档（想法入笔记机制依赖划线摘录块，不适用于无原文的书签备注；v2 候选：深链+备注、无引用文本的块形态）。
- 备注文本搜索（书签 tab 搜索框行为不变）。
- Markdown 渲染（备注为多行纯文本）。
- 备注的「编辑于」时间、版本历史（时间戳只管创建时刻）。
- 移动端专门适配（与现有书签创建入口一致，书签添加为桌面端功能）。
- 备注条数/长度的硬上限。

## Further Notes

- 词表增量已写入 `CONTEXT.md`：**书签**、**书签备注**、**备注串**（最新在上、全 CRUD、仅侧栏、删书签连带删除）。
- upsert-by-CFI 的字段保留（含新增备注字段）是实现本特性时必须一并修复的既有隐患。
- 会话事实：本机无 `gh` CLI / token / tracker 配置，spec 依仓内惯例本地落盘；配好 tracker 后按 `ready-for-agent` 标签发布。