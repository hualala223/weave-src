# Spec: 批量菜单全域一致 + 粘贴全部摘录 + 今日/未粘贴快捷圈定与已粘贴回写

Status: Finalized locally — 设计经两轮 grilling 定稿、用户已确认（含测试接缝：复用既有「批量粘贴块构建」纯函数缝，新增一个「摘录圈定/已粘贴回写」纯函数缝）。**待发布 issue tracker**（本机无 gh CLI / token / tracker 配置；配置好后按 `ready-for-agent` 标签发布）。实现等用户明确开工指令。

## Problem Statement

批量选择模式下的右键菜单因触发位置不同而残缺：在摘录列表顶部/面板空白处右键能看到「粘贴所选摘录到笔记」和「删除所选摘录」两项，但把列表拉到中部或底部、右键点在摘录卡片上时只剩「删除摘录」一项——用户无法在任意位置完成批量粘贴，感觉功能时有时无。

此外，读者想把整本书的摘录一次性搬进笔记（保持最早在上、最新在下的阅读顺序），目前只能先「全选当前结果」再粘贴，且该入口受面板筛选条件影响；也没有按时间（今天划的）或按状态（还没粘过的）快速圈定候选集合的手段。

## Solution

三件事：

1. **菜单一致性修复**：批量选择模式下，摘录卡片上的右键菜单与面板空白处的右键菜单展示同一套批量菜单项（粘贴所选摘录到笔记、删除所选摘录、全选/取消全选、退出批量选择）——无论在列表什么位置右键，两个核心操作都可用。批量模式下的单条「删除摘录」入口随之取消（退出批量模式后再做单条操作），语义统一为「对所选集合操作」。
2. **粘贴全部摘录**：非批量模式的面板右键菜单新增「粘贴全部摘录到笔记」，粘贴**本书全部摘录**（忽略面板筛选条件），createdTime 升序（最早在上）追加到当前笔记末尾，与既有粘贴所选摘录完全同格式。
3. **快捷圈定 + 已粘贴状态**：批量选择模式右键菜单新增「选中今日摘录」「选中未粘贴摘录」两项快捷圈定，均**替换**当前选择集合，用户再手动剔除不需要的条目后批量粘贴。为此摘录数据新增持久化的「已粘贴」标记：所有成功的粘贴路径（单条/批量/全部）在写入笔记成功后按实际成功条目回写；该标记仅作筛选依据，不阻止重复粘贴。

## User Stories

1. 作为读者，我想要在批量选择模式下于摘录卡片上右键时也能看到「粘贴所选摘录到笔记」，以便在列表任何位置都能发起批量粘贴，不必滚回顶部找空白处。
2. 作为读者，我想要批量模式下卡片右键与面板空白右键展示同一套菜单项，以便菜单行为可预期、不因位置而异。
3. 作为读者，我想要批量模式下的卡片右键不再出现单条「删除摘录」，以便批量模式内所有操作都对所选集合生效，不会误删单条。
4. 作为读者，我想要退出批量模式后卡片右键恢复原有单条菜单（批量选择/粘贴到笔记/删除摘录），以便单条操作入口不受影响。
5. 作为读者，我想要一键「粘贴全部摘录到笔记」，以便不必逐条勾选就能把整本书的摘录搬进笔记。
6. 作为读者，我想要粘贴全部时**忽略面板当前的搜索/筛选条件**，以便不被意外缩小范围。
7. 作为读者，我想要全部摘录按创建时间升序（最早在上、最新在下）写入笔记，以便笔记顺序与阅读先后一致。
8. 作为读者，我想要粘贴全部与粘贴所选输出完全同款的摘录块格式，以便笔记外观统一、深链都能跳回。
9. 作为读者，当本书没有任何摘录时，我想要「粘贴全部摘录到笔记」置灰，以便不会触发空操作。
10. 作为读者，当没有打开任何 Markdown 编辑器时，我想要粘贴全部只提示不写入（与既有粘贴同款提示），以便行为一致可预期。
11. 作为读者，我想要粘贴全部不弹确认、不设数量上限，完成后提示「已粘贴 N 条摘录到笔记末尾」，以便大量摘录时操作不被打断。
12. 作为读者，我想要批量模式下右键一键「选中今日摘录」，以便快速圈定今天划的内容作为粘贴候选。
13. 作为读者，我想要「今日」按本地自然日判定且仅限当前书，以便语义直观（今天零点以来划的）。
14. 作为读者，我想要批量模式下右键一键「选中未粘贴摘录」，以便接着上次的进度继续搬运而不会重复挑拣。
15. 作为读者，我想要快捷圈定**替换**当前已勾选集合，以便每次圈定都是一批新候选，不会被上次的勾选污染。
16. 作为读者，我想要圈定后仍可逐条取消勾选，以便剔除个别不想粘贴的条目后再粘贴。
17. 作为读者，我想要每次成功的粘贴（单条/批量/全部）把实际写入成功的摘录标记为已粘贴，以便「未粘贴」筛选自动闭环。
18. 作为读者，我想要部分摘录构建失败时不回写失败条目的已粘贴标记（成功的照常回写），以便失败条目下次仍出现在未粘贴候选里。
19. 作为读者，我想要已粘贴标记不阻止再次粘贴（粘贴不去重），以便重复搬运由我自己判断。
20. 作为读者，我想要旧书里既有的摘录（无标记）自动视为未粘贴，以便升级后功能立即可用、无需数据迁移。
21. 作为读者，我想要批量模式下三条粘贴路径（粘贴所选 / 合并粘贴所选 / 合并粘贴全部）成功后**自动取消实际写入条目的勾选**并保持选择模式，以便连续多批搬运且不必手动取消勾选。
22. 作为读者，我想要快捷圈定在无命中时清空选择并正常反馈，以便明确知道没有候选而不是界面无响应。

## Implementation Decisions

- **菜单路由修复**：批量选择模式下，摘录卡片的右键菜单改为渲染批量菜单项集合（与面板空白处 `showPanelContextMenu` 的 selectionMode 分支同一套：粘贴所选/删除所选/全选/取消全选/退出）；实现上是摘录卡片右键处理函数在 selectionMode 下走批量菜单分支，而非依赖面板根冒泡（卡片右键本就 `stopPropagation`，这是缺陷根源）。非批量模式卡片菜单维持现状。
- **粘贴全部入口**：非批量模式面板右键菜单新增「粘贴全部摘录到笔记」，禁用条件：本书无摘录 / 宿主回调缺失 / 粘贴进行中。数据源为本书全部摘录集合（面板数据聚合中的全集，非当前筛选结果）。
- **复用既有粘贴链路**：三条粘贴路径（单条/所选/全部）共用同一宿主函数与「批量粘贴块构建」纯函数服务，行为（升序、原始创建时间戳、想法条目内联、末尾追加、单一写入、失败条目跳过、不去重）完全一致；新增入口只是换数据源。
- **粘贴后自动取消勾选（2026-09-22 修订）**：批量模式内三条粘贴路径（粘贴所选 / 合并粘贴所选 / 合并粘贴全部）写入成功后，宿主按块构建服务回传的 `keys`（= cfiRange 口径，与「已粘贴」标记回写同一成功口径）从面板 `selectedKeys` 剔除已写入条目；构建失败的条目保持勾选以便立即重试；整批未写入（如无编辑器）不动任何勾选；面板保持在批量选择模式（可继续圈定/勾选下一批）。
- **快捷圈定**：批量菜单新增两项，行为为「替换当前选择集合」。圈定条件为纯谓词：今日 = 摘录 createdTime 的本地自然日等于今天（注入当前时刻以便测试）；未粘贴 = 摘录无已粘贴标记。
- **已粘贴标记（schema 变更）**：摘录记录新增一个可选字段（最近一次成功粘贴时间）；字段缺省 = 未粘贴，向后兼容、无需迁移。回写时机：宿主粘贴函数在写入编辑器**成功之后**，仅对实际构建成功并写入的条目回写，并走既有摘录存储持久化链路。标记无撤销、无 UI 展示（仅作筛选谓词输入）。
- **共享回调**：面板到宿主新增/扩展粘贴回调的入参以携带「圈定子集」即可，无需新增平级回调（全部粘贴 = 传入全集）。
- **术语已固化**：「已粘贴」「今日摘录」已录入 CONTEXT.md；「粘贴所选摘录」条目已更新（菜单全域一致、粘贴全部、快捷圈定两个新入口）。

## Testing Decisions

- **好测试的标准**：只测外部行为——纯函数给定输入（摘录快照列表 + 注入的当前时刻/上下文）断言输出（圈定子集、回写后的记录集、粘贴块文本），不给内部实现留断言；不测 svelte 菜单/宿主胶水。
- **测试缝（两处）**：
  1. **复用既有缝**：「批量粘贴块构建」纯函数服务——粘贴全部不引入新缝，只是换输入集合；既有测试直接覆盖。条目新增可选 key（口径 = cfiRange），结果回传实际构建成功的 key 集合，供宿主精确回写。
  2. **新增唯一缝**：「摘录圈定」纯函数——输入摘录快照列表与当前时刻，输出今日/未粘贴子集。已粘贴回写本身走宿主既有划线变更队列（按 cfiRange patch 落盘），属宿主胶水不单测；菜单路由不单测。
- **先例**：`excerpt-batch-paste` 的既有测试（排序/拼接断言风格）、`idea-note-doc.test.ts`（纯函数给定输入→断言输出）、schema 既有 round-trip 测试（新字段向后兼容用例）。

## Out of Scope

- 粘贴去重、防重复粘贴拦截、已粘贴标记的撤销或 UI 展示。
- 快捷圈定的「追加」模式（定案为替换）；圈定条件自定义（如本周、某章节）。
- 跨书粘贴（仅当前书）；图片记录、书签备注等非划线条目的粘贴。
- 面板常驻粘贴按钮（定案为右键菜单入口）。
- 批量模式下单条删除的替代入口（退出批量后操作）。
- 国际化/文案配置化。

## Further Notes

- 菜单缺陷根因备忘：卡片右键处理器阻断冒泡到面板根，且其菜单内粘贴/批量项被「非批量模式」条件包裹，导致批量模式下卡片右键只剩删除——修复是让批量模式下的卡片右键直接渲染批量菜单集合，而非移除冒泡阻断。
- 与既有输出链路区分：自动插入（划线创建时）、想法入笔记（想法保存时）、粘贴所选/全部（用户从面板主动发起）三者格式一致、可安全共存。
- 2026-09-22 修订：经 grilling 确认（Q1-Q4 全按推荐），「粘贴成功后保持选择模式」由「保持选择模式 + 自动取消实际写入条目的勾选」取代——原决策保留连续多批搬运的意图，新增自动清勾选消除手动取消的负担；撤销的是 SPEC-paste-excerpts-to-note US8 与本 spec US21 的「勾选保留」半边。取消口径与「已粘贴」回写完全一致（按回传 keys），不引入新规则。
- 发布说明：本机无 gh CLI / tracker 配置，按仓库先例标注「待发布 issue tracker」，配置好后按 `ready-for-agent` 标签发布。
