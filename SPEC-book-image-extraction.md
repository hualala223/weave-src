# Spec: 书内图片提取同步到笔记文档 (In-book Image Extraction to Note Document)

Status: Finalized locally — user confirmed the seam and chose **not** to publish to any issue tracker. Implementation awaits the user's explicit go-ahead.

## Problem Statement

读电子书时，读者经常想把书里的插图（图表、地图、照片）收集到自己的读书笔记里。目前插件只能对**文字**做划线并在 MD 笔记文档里生成摘录块；图片没有任何提取手段——读者只能手动截图、手动找文件，笔记里无法保留书内插图的原始质量与出处位置。

用户诉求：选择书里的图片，像文字划线一样，自动同步到（书本文件之外的）打开的 MD 笔记文档中。

## Solution

在阅读界面中，点击书内图片会浮出一个小操作条，点击"提取到笔记"后：

1. 从书籍档案中读出该图片的**原始字节**（EPUB 用内存档案直读，不依赖渲染层）。
2. 以 `图片附件` 形式写入 Obsidian vault 的 `<vault根>/CONFIG/asset/<书名>/` 目录，文件名为 `<书名>-<章节号>-<序号>.<ext>`（序号递增保证唯一）。
3. 在打开的 `笔记文档` 的**末尾**追加一条 `图片记录`：图片嵌入行 + 一行纯文本 `深链`/章节信息（无 callout 包装）。
4. 点击 `深链` 可跳回书内该图所在位置。

该动作始终插入笔记文档，不受"自动化"开关影响；重复提取同一张图会再生成一个唯一文件并再次追加；v1 不写入阅读器内笔记面板、不改存储结构、不自动清理附件。

## User Stories

1. 作为读者，我想要点击书内图片并把它提取到打开的笔记文档中，以便把书内插图与文字划线一起收集。
2. 作为读者，我想要提取成功后在笔记文档中看到**嵌入的原图**（而非链接或占位符），以便笔记里直接呈现插图本身。
3. 作为读者，我想要图片附件存放在 vault 根目录 `CONFIG/asset/<书名>/` 这个可预期、以书命名的文件夹中，以便日后查找和复用这些图片文件。
4. 作为读者，我想要附件文件名由"书名-章节号-序号"构成且互不重复，以便文件名可读、可辨识、绝不冲突。
5. 作为读者，我想要图片记录里带一个深链，点击能跳回书内该图所在位置，以便在笔记与书之间来回导航。
6. 作为读者，我想要图片记录里显示章节名（遵循现有章节标签设置），以便知道图片来自哪里。
7. 作为读者，我想要在启用"添加创建时间"设置时，图片记录也带时间戳（与文字摘录一致），以便追溯。
8. 作为读者，我想要同一张图可以反复提取（不做去重），以便同一插图可以进多个笔记文档。
9. 作为读者，我想要提取动作**不受"自动化"开关影响**、始终直接插入笔记文档，以便行为可预期。
10. 作为读者，我想要提取结果**追加到笔记文档末尾**（而非光标处），以便图片自然地堆在笔记尾部。
11. 作为读者，我想要点击图片后先出现一个操作条、含明确的"提取"动作，以免误触直接插入。
12. 作为读者，我想要在没有打开任何 MD 笔记文档时收到提示（且不产生副作用），以便我明白为什么没插入内容。
13. 作为移动端读者，我想要在分页模式下点击图片触发提取而不是翻页，以便该功能在手机上可用。
14. 作为读者，我想要图片的 alt/图注信息被保留（作为嵌入的 alt 别名），以便不丢失图片说明。
15. 作为阅读含 SVG 等图片的读者，我想要附件扩展名与图片 MIME 类型匹配，以便 Obsidian 能正确渲染。
16. 作为阅读 MOBI/AZW3 等格式的读者，我想要提取在脆弱路径下优雅降级（回退读取或明确提示），以免影响阅读。
17. 作为读者，我想要删除笔记里的图片行**不会**自动删除 vault 附件文件，以免误失文件。
18. 作为书名含文件名非法字符的读者，我想要目录名与文件名经过净化，以免提取失败。
19. 作为读者，我想要桌面端与移动端的提取行为一致。
20. 作为读者，我想要图片记录的深链正确引用当前书文件在 vault 中的路径，以便从任意笔记点击都有效。

## Implementation Decisions

- **单一新模块：图片提取服务**。拥有提取全链路与全部决策逻辑，注入依赖（vault 二进制写入适配器、现有链接服务、摘录设置）。对外接口：输入 `{bookId, bookTitle, chapterIndex, chapterTitle, chapterLabel, cfi, alt?, mimeType, bytes}`，返回 `{attachmentPath, block}`。调用方（阅读器应用层处理器）退化为薄胶水，与现有划线→摘录流程对称。
- **附件路径与命名**：目录 = vault 根 + `CONFIG/asset/<净化书名>/`（对齐插件现有 CONFIG 目录的解析方式）；文件名 = `<净化书名>-<章节号>-<序号>.<扩展名>`；扩展名由 MIME 推导；序号为跨会话递增的计数器，保证允许重复策略下文件名仍然唯一；书名做文件名净化（非法字符替换）。
- **书内图片 → 档案资源反查**：渲染管线会把章节内 `<img src>` 改写为 data:/blob: URL，因此需要新增一个 src→manifest href 反查步骤（基于已处理章节文档的扫描或相对 src 对照章节 href 解析）。EPUB 原图字节从内存档案按 href 直读（`uint8array`）+ MIME 推断；非 EPUB/档案读取失败时回退到 blob/data-URI 读取；全部失败则提示且不写附件。
- **阅读器引擎能力**：在阅读器引擎接口上新增图片捕获回调（在某帧 DOM 内点击 `<img>` → 浮出操作条 → 确认后回调，负载含 href、CFI、章节号/章节名、alt）。按帧绑定与清理的生命周期完全复用现有选择/翻页监听器模式。
- **移动端协调**：分页翻页模式下，点击命中 `<img>` 的路由优先进入提取流程而非翻页。
- **笔记插入**：新增"追加到文档末尾"的插入行为（不同于现有光标处插入）；未找到打开的 MD 编辑器时提示且不产生任何写的副作用。
- **块格式**（已与用户确认）：图片嵌入行 + 一行纯文本深链/章节/时间，无 callout 包装；时间行遵循现有"添加创建时间"设置；有 alt 时通过嵌入别名保留。
- **行为规则（均已确认）**：允许重复提取；不受"自动化"开关门控；不写阅读器内笔记面板记录；不改存储 schema；不自动清理附件。
- **现有资产复用**：深链构建复用现有链接服务（不改其 API）；blob 字节读取复用现有 registry 作回退；操作条复用现有浮动菜单组件；不新增依赖。

## Testing Decisions

- **好测试的标准**：只测外部行为——给定服务输入（含伪造的 vault 写入适配器与链接服务），断言附件路径/命名、写入的字节、返回块文本的形状；不触碰 iframe DOM 内部实现。
- **被测模块**：图片提取服务（新）——覆盖路径解析、文件名净化与序号唯一性、字节写入、块文本（嵌入行 + 深链/章节/时间行、alt 别名、时间设置开关）、无编辑器/读取失败等错误分支。
- **测试先行依据（现有同类测试）**：`EpubLinkService.test.ts`（服务 + 伪造依赖、断言块字符串——最贴近的模式）、`blob-url-registry.test.ts`（工具级字节处理）、`EpubView.test.ts`（伪造 Obsidian 类观察编辑器内容——用于"追加文末"路径如被暴露到视图层）。
- **不测的范围**：Foliate iframe 内监听器接线（与现有选择/翻页监听器一致地视为未测试胶水）；Obsidian vault 写入本身（一律伪造）。

## Out of Scope

- 图片进入阅读器内笔记面板的卡片展示（v1 不做）。
- 图片注解的持久化与 schema 变更（v1 不改存储结构）。
- 删除笔记行时自动清理附件文件。
- 批量/多选提取。
- PDF 等 Premium 格式（仅对同引擎格式做尽力回退）。
- 跟随 Obsidian 附件目录设置（v1 固定 `CONFIG/asset/<书名>/`；设置项留待后续）。
- 封面图提取（与书内图片是不同概念）。

## Further Notes

- 术语见 `CONTEXT.md`（划线、摘录块、笔记文档、笔记面板、深链、书文件、书内图片、提取、图片附件、图片记录）。
- 可行性依据：`FEASIBILITY-inbook-image-note.md`（DOM 可达、原图字节三路径、元素 CFI、平台差异）与 `TEXT-HIGHLIGHT-PIPELINE-REPORT.md`（划线→笔记全链路、存储与排序、唯一 vault 写入点）。
- 待记录 ADR：附件存放位置（插件首次写 vault 附件、不随 Obsidian 附件设置、固定 `CONFIG/asset/<书名>/`）符合 ADR 三条标准，开工时记录为 `docs/adr/0001-image-attachment-location.md`。
- 发布说明：spec 需发布到项目 issue tracker 并打 `ready-for-agent` 标签；当前会话缺少 tracker/标签词汇，需先运行 `/setup-matt-pocock-skills`。