# Spec: 想法入笔记——写完想法自动沉淀原文与想法到笔记文档末尾 (Idea-to-Note)

Status: Finalized locally — 设计经三轮 grilling 定稿、用户已确认测试接缝。**待发布 issue tracker**（本机无 gh CLI / token / tracker 配置；配置好后按 `ready-for-agent` 标签发布）。实现等用户明确开工指令。

## Problem Statement

读电子书时会针对某句话写下想法（想法）。目前想法只存进阅读器的标注库：笔记文档里什么都不会留下。想把「原文 + 我的想法」收进读书笔记，必须在写完想法后再手动做一次摘录输出——而手动输出根本不带想法内容。更糟的是，对同一句话重写想法时，旧想法会在标注库里被直接覆盖丢失。

用户诉求：写完想法之后，被划线的原文文字和写下的想法能自动进入笔记文档最下面，像划线输出一样；放置时清楚区分原文内容和想法内容。

## Solution

保存一条非空想法后，插件自动在最近激活的笔记文档**末尾**追加一个摘录块：块内先是引用的原文（与手动输出完全一致的头部与深链），其下以粗体 💡 标签引出想法内容，每条想法自带时间戳。

- 第一次写想法 → 创建整块；
- 之后对同一句再写想法 → 在该块已有想法条目下方**追加**新条目（历史累积，绝不替换）;
- 通过「编辑想法」修改 → 只原位改写**最后一条**条目；
- 把想法改空 → 剥掉最后一条，块退化为纯摘录块；
- 保存内容与现状相同 → 什么都不动。

由独立开关控制（默认开），与「自动化」(zap) 互不影响；找不到打开的 Markdown 编辑器时兜底复制整块到剪贴板并提示。

目标块形态（grilling 中用户确认）：

```
> [!EPUB|underline] [[书#…weave-loc…&eid=X|原文…]] 第三章 · 2025-01-01
> 被划线的原文……
>
> **💡 想法：** 01-01 22:10
> A1
>
> **💡 想法：** 01-02 09:30
> A2
```

## User Stories

1. 作为读者，我想要在写完想法保存后，被划线的原文和我写的想法自动出现在笔记文档的最下面，以便专注阅读时想法不丢、笔记自动积累。
2. 作为读者，我想要笔记里一眼区分哪些是书里的原文、哪些是我自己的想法，以便日后复习时不混淆出处与心得。
3. 作为读者，我想要想法块追加到笔记文档的**末尾**而不是光标处，以便它像流水一样堆在笔记尾部、不打断当前写作位置。
4. 作为读者，我想要想法块的头部与我手动输出的摘录块完全一致（深链、章节标签、样式标记），以便两种来源的块外观统一、点击都能跳回书中位置。
5. 作为读者，我今天对句子 A 写了想法 A1，明天再看同一句想写 A2 时，我想要 A2 追加在 A1 下面而不是替换它，以便想法随时间累积成思考轨迹。
6. 作为读者，我想要每条想法自带时间戳，以便分辨不同日子写下的多条想法。
7. 作为读者，我想要通过「编辑想法」修正错别字时只改动最新那条想法，之前的历史条目原封不动，以便历史不被意外篡改。
8. 作为读者，我想要保存内容与现有内容完全相同时什么都不发生，以便重复点保存不会在笔记里制造重复块。
9. 作为读者，我想要把想法清空再保存后，笔记里对应块退化成纯摘录块（只剩原文），以便笔记与我的真实状态一致、不留脏数据。
10. 作为读者，我想要多行想法逐行保留在块内，以便长想法不丢换行结构。
11. 作为读者，我想要想法块头部的章节信息遵循现有「章节定位」格式设置，以便与既有摘录一致。
12. 作为读者，我想要块头时间戳继续跟随「摘录时间戳」开关，且原位更新时尽量保留最初的记录时间，以便头部反映"何时划的"而非"何时改的"。
13. 作为读者，我想要一个独立的「想法自动入笔记」开关且默认开启，以便不喜欢该行为的人可以彻底关掉。
14. 作为读者，我想要关闭该开关后写想法完全不产生任何笔记写入或剪贴板动作，以便行为干净可预期。
15. 作为读者，我想要该功能不受「自动化」(zap) 开关影响，以便两个互不相干的行为各自独立。
16. 作为读者，当没有打开任何 Markdown 编辑器时，我想要整块内容兜底复制到剪贴板并收到提示，以免刚写的想法凭空消失。
17. 作为读者，当我删掉了笔记里的旧块或在另一份笔记里输出过，再次编辑想法时我想要在当前笔记末尾重新追加一份，以便功能永远有确定的结果而不跨文件乱改。
18. 作为读者，对于本功能上线前手动输出的旧摘录块，我想要它们不被误认成想法块，以便编辑想法时生成新的规范块而不破坏旧内容。
19. 作为读者，我先普通划了线、后来才补写第一条想法时，我想要照常触发首次插入，以便两种高亮来源体验一致。
20. 作为读者，我想要修改划线颜色/样式不会追认改动笔记里的块，以便笔记是单向快照流、行为简单。
21. 作为读者，我想要删除划线后笔记里的块保留不动，以便笔记内容不会被远程破坏。
22. 作为读者，我对同一句重写想法时，我想要划线本身保留原有的身份标识与创建时间（而不是被当成全新划线），以便笔记面板记录稳定、深链长期有效。
23. 作为读者，我想要笔记面板显示该划线最新的想法，以便阅读器内信息与标注库一致。
24. 作为读者，我想要本功能输出的深链携带稳定的划线标识，以便后续编辑能可靠找到同一块。
25. 作为读者，我想要开关入口就在阅读器菜单「摘录笔记设置」子菜单里与其他摘录设置并列，以便一处管理所有摘录行为。

## Implementation Decisions

- **单一新模块：想法入笔记文档变换服务**（唯一新接缝）。纯函数式的"文档文本进、文档文本出"，不含 Obsidian 依赖，注入所需的构造参数。对外能力：
  - 由摘录块文本 + 想法条目集合构建完整块文本；
  - 按**稳定划线标识（eid）优先、CFI 兜底**在目标文档文本中定位旧块，返回块区间或 null；
  - upsert 语义：返回结果枚举 `created / appended / replaced / stripped / noop` 及新文档全文——created=新建整块、appended=追加新条目、replaced=改写最后一条、stripped=剥掉最后一条、noop=内容相同零改动；
  - 同 CFI 重写时的记录合并规则：保留原 excerptId 与 createdTime，仅更新文本/样式/评论位。
- **摘录块构建服务的最小扩展**：新增可选的真实 excerptId 入参并序列化进深链（现有链路已支持 eid 子路径）。仅本流程传真实值；既有手动输出路径保持现状（每次随机 eid），因此旧行为零变化。
- **接线保持薄胶水**：想法保存路径调用变换服务得到新文档文本后，经现有「追加到文档末尾」插入工具写入最近激活的 Markdown 视图；插入失败或无编辑器时改为剪贴板复制并提示。创建/编辑两条 UI 路径分别映射为 appended / replaced 语义。
- **设置项**：加入摘录设置的持久化结构（布尔、默认开），走既有的归一化通道；阅读器菜单「摘录笔记设置」子菜单新增同级勾选项。关闭时整个想法→笔记链路短路。
- **格式细节（均已确认）**：想法段以空引用行分隔；粗体标签内含 💡 图标；每条条目自带紧凑时间戳（月-日 时:分），**独立于**「摘录时间戳」开关（后者继续只管块头）；多行想法逐行转为引用行。
- **清空语义**：编辑为空并保存 = stripped（允许的现状不变，仅笔记侧表现确定）。
- **术语已固化**：「想法」「想法条目」「想法入笔记」已录入 CONTEXT.md；同时修正了「自动插入」定义与代码不符处（文字摘录实际插光标处、图片记录才是末尾追加）。

## Testing Decisions

- **好测试的标准**：只测外部行为——给定输入字符串/对象，断言返回的 outcome 枚举与输出文档全文（或精确片段）；不给内部实现留断言。UI 组件胶水不做单测（沿仓库惯例）。
- **被测模块**：
  - 新变换服务：覆盖全部五态语义（含同句二次写入追加、编辑改写最后一条、清空剥离、完全相同 noop）、eid 命中 / CFI 兜底 / 全部失配→null、旧块头时间戳保留、同 CFI 合并规则的身份保留。
  - 摘录块构建服务：真实 excerptId 序列化进深链的新分支（其余既有用例保持不动）。
  - 「追加到末尾」插入工具：既有测试保持绿，不改。
- **Prior art**：epub 服务测试目录下现有的链接构建器测试（纯字符串格式断言）与编辑器插入工具测试（deps 注入伪造编辑器）——新测试完全沿用这两类模式与运行器（vitest）。

## Out of Scope

- 跨文件/全库检索旧块（只搜当前插入目标文档；别处的旧块一律视为不存在）。
- 双向同步：改颜色/样式、删除划线不追认笔记；无删除墓碑。
- 标注库多想法存储（schema 保持单评论位"最新获胜"；历史只存在于笔记文档）。
- 笔记面板展示全部历史想法（面板只显示最新）。
- 对旧随机 eid 块的原位升级改造。
- 独立的设置页入口（仅阅读器菜单开关）。

## Further Notes

- **连带修复**：现状下对同一句重写想法会在持久化瞬间抹掉旧记录（含面板中的想法）——本 spec 的身份保留合并规则即修复此缺陷，是"追加"语义成立的前提。
- **ADR 候选**：「库中存最新、笔记存历史」的单向快照模型满足难逆转/无上下文会困惑/真实取舍三条件，建议补一篇 ADR（编号顺延 docs/adr 现有序列），待用户点头。
- 发布迁移：tracker 就绪后将本文原样发往 issue tracker 并打 `ready-for-agent`。
