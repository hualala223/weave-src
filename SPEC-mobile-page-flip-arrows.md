# Spec: 移动端阅读翻页箭头——长按拖动定位的上下翻页浮层

Status: Finalized locally — 两轮 grilling 定稿（Q1/Q2/Q3/Q6 采推荐项；Q4 默认左下角；Q5 长按拖动+快速单击翻页；Q7–Q10 采推荐项）。待实现。

## Problem Statement

桌面端阅读界面右侧有向上/向下箭头可点击翻页（滚动模式），移动端完全没有这个入口，读者只能靠滑动手势翻页，单手阅读时不方便。

## Solution

移动端阅读界面新增一组浮动的向上/向下箭头（整体胶囊、纵向排列），快速单击即翻一页；长按后可整体拖动到屏幕任意位置，位置被记住，下次打开还在原地。默认出现在左下角（单手持机拇指易达）。

## Decisions（grilling 定稿）

- **显示条件**：仅滚动模式（scrolled flow）下显示，逻辑对齐桌面端侧边导航（滚动模式判定 + 显示开关），与桌面垂直导航 `showBottomNav()/useVerticalNav()` 的组合语义一致；移动端阅读以滚动模式为主，非滚动模式不显示。
- **形态**：两个箭头（上/下）作为一个不可拆分的整体胶囊组，纵向排列，浮于正文之上，半透明常显。
- **翻页动作**：调用与桌面箭头完全相同的翻页服务入口（上一页/下一页），不新写翻页逻辑。
- **交互状态机**：
  - 快速单击（pointerup 前未达长按时长、未位移超阈值）→ 翻页；
  - 按住达到长按时长 → 进入可拖动态（视觉反馈：微微放大、变为实心），此后移动即整体拖动；
  - 按住但未达长按就开始滑动 → 无操作（不跟随、不翻页，避免与正文滚动手势混淆）；
  - 松手 → 恢复半透明，退出拖动态。
- **拖动范围**：视口内自由停放，仅做视口边缘 clamp（不越界），不做边缘吸附。
- **位置记忆**：拖动后的位置存入阅读器设置；该设置层本就按 mobile/desktop 分设备存储，因此只影响移动端。从未拖过时默认位置为**左下角**。
- **开关**：阅读设置菜单新增独立布尔项「移动端翻页箭头」，默认开启；关闭后不渲染该浮层。

## Implementation

- 新建移动端翻页箭头组件（Svelte，参考桌面垂直导航组件的图标与按钮语义，icon 沿用上/下箭头），由阅读器主应用在移动端 + 滚动模式 + 开关开启时挂载，定位边界以阅读视口容器为准（复用现有弹层定位的 bounds 约定）。
- 新建纯函数缝（手势与定位逻辑）：长按/单击/滑动判定（长按时长、位移阈值常量）与箭头组位置 clamp 计算，输入事件语义与视口尺寸、输出动作（翻页/无操作）与停放坐标。组件只做事件接线与渲染。
- 阅读器设置结构新增两个字段：开关布尔（默认 true）与停放位置（可为空表示"未拖过 → 默认左下角"），走现有阅读器设置的归一化与按设备持久化通道，向后兼容（旧数据无字段 = 默认值）。
- 阅读设置菜单增加勾选项入口，与现有阅读设置项同级。

## Testing

- 好的测试只测外部行为：给定事件序列（按下→抬起等）判定动作，给定视口尺寸与任意坐标返回 clamp 后的合法坐标；不测 Svelte 接线。
- 新增纯函数缝的单测（vitest），先例：`excerpt-display-order.test.ts`、`toolbar-positioning.test.ts`（纯函数/纯计算缝 + 组件不测）。

## Out of Scope

- 桌面端箭头的行为与样式改动；非滚动模式下的翻页箭头；两个箭头各自独立拖动；边缘吸附/网格对齐；箭头大小、透明度、长按时长等用户可调项；横排摆放方案（左下角方案已可行，无需回退）。
